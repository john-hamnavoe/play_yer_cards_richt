<ol class="breadcrumb">
  <li class="breadcrumb-item active mr-auto">Game_questions</li>
</ol>
<div class="card-columns">
  <% @game_questions.each do |game_question| %>
    <div class="card card mb-4">
      <div class="card-body">
        <h5 class="card-title"><%= link_to game_question.to_s, game_question %></h5>
        <p class="card-text"><%= game_question.created_at %></p>
      </div>
    </div>
  <% end %>
</div>
